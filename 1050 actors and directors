-- LeetCode 1050: Actors and Directors Who Cooperated At Least Three Times
-- Find (actor_id, director_id) pairs who worked together 3+ times.

SELECT actor_id, director_id
FROM ActorDirector
GROUP BY actor_id, director_id
HAVING COUNT(*) >= 3;
