-- LeetCode 183: Customers Who Never Order
-- Find customers with no matching row in Orders, using LEFT JOIN +
-- IS NULL to detect "no match found."

SELECT c.name AS Customers
FROM Customers c
LEFT JOIN Orders o
    ON c.id = o.customerId
WHERE o.customerId IS NULL;
